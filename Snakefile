
filterout = '{FILTEROUT}'
filterin = '{FILTERIN}'
data = '{DATA}'
threads = {THREADS}

rule all:

rule build_databases:

    ##[Organism:exp]


rule trimmomatic:

rule flash:

rule filter_out:

rule filter_in:

rule run_trinity:

rule blast_contigs:
